[tox]
isolated_build = true
envlist = py39-spark3{2,3}

[testenv]
setenv =
    TZ=UTC
whitelist_externals = poetry
deps =
    spark32: delta-spark<2.0.0
    spark33: delta-spark==2.0.0
    pytest
    pytest-cov
    moto
commands =
    pytest --cov=getl
